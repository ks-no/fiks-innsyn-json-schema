{
  "id": "https://fiks.ks.no/innsyn/matrikkelenhet.v1.json",
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "Innsyn Matrikkelenhet V1",
  "description": "Melding som beskriver en matrikkelenhet",
  "type": "object",
  "required": [
    "matrikkelnummer",
    "matrikkelenhetId"
  ],
  "definitions": {
    "MatrikkelenhetMatrikkelnummerV1": {
      "type": "object",
      "required": [
        "kommunenummer",
        "gardsnummer",
        "bruksnummer",
        "festenummer",
        "seksjonsnummer"
      ],
      "properties": {
        "kommunenummer": {
          "type": "string",
          "pattern": "^\\d{4}$"
        },
        "gardsnummer": {
          "type": "string",
          "pattern": "^[0-9]{1,6}$"
        },
        "bruksnummer": {
          "type": "string",
          "pattern": "^[0-9]{1,6}$"
        },
        "festenummer": {
          "type": "string",
          "pattern": "^[0-9]{1,6}$"
        },
        "seksjonsnummer": {
          "type": "string",
          "pattern": "^[0-9]{1,6}$"
        }
      }
    }
  },
  "properties": {
    "matrikkelnummer": {
      "$ref": "#/definitions/MatrikkelenhetMatrikkelnummerV1"
    },
    "matrikkelenhetId": {
      "$ref": "int64.json",
      "description": "Matrikkel-intern id for matrikkelenheten"
    },
    "adresser": {
      "description": "Adresse for matrikkelenheten",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      },
      "examples": [
        [
          "Øvre Testveien 34, 3765 Storevik"
        ],
        [
          "Nedre Testveien 9",
          "Nedre Testveien 11"
        ]
      ]
    },
    "korrelasjonId": {
      "$ref": "korrelasjon.id.json"
    },
    "forelder": {
      "$ref": "forelder.json"
    }
  }
}